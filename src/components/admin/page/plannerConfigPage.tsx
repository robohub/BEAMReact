import * as React from 'react';
import PlanConfigView from './planner/planConfigTableView';

export default class PlannerConfig extends React.Component {
    render() {  
        return (
            <PlanConfigView/>
        );
    }
}  